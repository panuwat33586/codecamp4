export function main(str, str2) {
	let A=0;
	for(let i=0;i<str.length;i++){
	switch(str[i]){
	case str2[i]:
		;
		break;
		default:
			A++    
	}
	};
	return A  
function main(str, str2) {
	// Your code begins here;
}

module.exports = { main };
